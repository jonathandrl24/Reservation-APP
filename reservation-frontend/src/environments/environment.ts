/** Production / default build: set `apiUrl` to your deployed API or inject at deploy time (see `environment.development.ts` for local dev). */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080',
};
